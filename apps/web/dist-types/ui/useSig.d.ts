import type { Signal } from '@preact/signals';
/** Explicit subscription to a signal: re-renders the component when it changes. */
export declare function useSig<T>(sig: Signal<T>): T;
//# sourceMappingURL=useSig.d.ts.map