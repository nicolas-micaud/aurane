# Copy to terraform.tfvars (gitignored). Secrets never go here except the two bootstrap tokens,
# which are single-use / revocable; prefer TF_VAR_* env vars loaded from Vaultwarden (README).
ssh_public_key = "ssh-ed25519 AAAA... ops@gmk1"
# tailscale_authkey = "tskey-auth-..."   # mint per apply, tags ["tag:aurane"], ephemeral, 1 use
# cloudflared_token = ""                  # dedicated tunnel token once created
